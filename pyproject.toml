[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "wrapping"
version = "0.1.0"
description = "Callable decorator module for local exercises"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["wrapping"]