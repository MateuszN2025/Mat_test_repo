[pytest]
; Directory containing test files
testpaths = tests
; Additional command-line options: -ra reports summary of all test outcomes
addopts = -ra
markers =
	optimal: marks optimal test cases


; pytest.ini
; project-level pytest settings
;   Typical uses:
;   where tests live
;   default command options
;   markers
;   warning filters
;   naming conventions
