[pytest]
testpaths = tests
addopts = -ra

; pytest.ini
; project-level pytest settings
;   Typical uses:
;   where tests live
;   default command options
;   markers
;   warning filters
;   naming conventions
