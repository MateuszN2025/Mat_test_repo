JENKINS_URL=http://127.0.0.1:8080/
AGENT_NAME=local-agent
AGENT_SECRET=replace-with-agent-secret
AGENT_WEB_SOCKET=true
# Optional: set when the controller is reachable through a tunnel host:port pair.
AGENT_TUNNEL=
# Optional: override the Java executable.
JAVA_BIN=java
